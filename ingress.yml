apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress
  annotations:
    kubernetes.io/ingress.allow-http: "false"
    kubernetes.io/ingress.global-static-ip-name: ticket-app-dev
    networking.gke.io/managed-certificates: google-certificate
    # cert-manager.io/cluster-issuer: letsencrypt-issuer
spec:
  backend:
    serviceName: ticket-app-dev-service
    servicePort: 80
  # rules:
  #   - host: ticket-app-dev.tk
  #     http:
  #       paths:
  #       - backend:
  #           serviceName: ticket-app-dev-service
  #           servicePort: 80
  #         path: /
  tls:
  - hosts:
    - ticket-app-dev.tk
  #   secretName: google-certificate