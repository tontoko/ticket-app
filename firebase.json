{
  "functions": {
    "source": "functions",
    "predeploy": [
      "npm --prefix \"$PROJECT_DIR/functions\" install",
      "npm --prefix \"$PROJECT_DIR/functions\" run build"
    ]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
